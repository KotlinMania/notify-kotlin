# Port targets — `notify-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `notify` that codex-rs uses, and therefore the minimum subset that `notify-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-82] (rank `209`, tier `direct`, status `To Do`)

**Cargo package name**: `notify`
**Rust module name (in `use` statements)**: `notify`

## Summary

- **9** distinct symbols imported by codex-rs
- **13** total `use` statements
- **3** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `notify::RecursiveMode`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `core_test_support`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher.rs:18` (`codex-core`) — `use notify::RecursiveMode`
- `core/tests/common/lib.rs:390` (`codex-core`) — `use notify::RecursiveMode`
- `core/tests/common/lib.rs:390` (`core_test_support`) — `use notify::RecursiveMode`

### `notify::Watcher`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `core_test_support`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher.rs:19` (`codex-core`) — `use notify::Watcher`
- `core/tests/common/lib.rs:391` (`codex-core`) — `use notify::Watcher`
- `core/tests/common/lib.rs:391` (`core_test_support`) — `use notify::Watcher`

### `notify::event::AccessKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher_tests.rs:2` (`codex-core`) — `use notify::event::AccessKind`

### `notify::event::AccessMode`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher_tests.rs:3` (`codex-core`) — `use notify::event::AccessMode`

### `notify::event::CreateKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher_tests.rs:4` (`codex-core`) — `use notify::event::CreateKind`

### `notify::event::ModifyKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher_tests.rs:5` (`codex-core`) — `use notify::event::ModifyKind`

### `notify::Event`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher.rs:14` (`codex-core`) — `use notify::Event`

### `notify::EventKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher.rs:16` (`codex-core`) — `use notify::EventKind`

### `notify::RecommendedWatcher`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/file_watcher.rs:17` (`codex-core`) — `use notify::RecommendedWatcher`
