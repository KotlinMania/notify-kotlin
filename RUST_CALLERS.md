# Rust callers — `notify-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `notify` | `notify` | 13 | 2 | 1 | 13 |

## `notify`

### Inbound symbols (9 distinct, 13 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `notify`.

#### `notify::RecursiveMode` — 3 site(s) (3 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher.rs:18` — `use notify::RecursiveMode`
- ✅ `codex-kotlin/codex-core` `tests/common/lib.rs:390` — `use notify::RecursiveMode`
- ✅ `codex-kotlin/core_test_support` `lib.rs:390` — `use notify::RecursiveMode`

#### `notify::Watcher` — 3 site(s) (3 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher.rs:19` — `use notify::Watcher`
- ✅ `codex-kotlin/codex-core` `tests/common/lib.rs:391` — `use notify::Watcher`
- ✅ `codex-kotlin/core_test_support` `lib.rs:391` — `use notify::Watcher`

#### `notify::Event` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher.rs:14` — `use notify::Event`

#### `notify::EventKind` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher.rs:16` — `use notify::EventKind`

#### `notify::RecommendedWatcher` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher.rs:17` — `use notify::RecommendedWatcher`

#### `notify::event::AccessKind` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher_tests.rs:2` — `use notify::event::AccessKind`

#### `notify::event::AccessMode` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher_tests.rs:3` — `use notify::event::AccessMode`

#### `notify::event::CreateKind` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher_tests.rs:4` — `use notify::event::CreateKind`

#### `notify::event::ModifyKind` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/file_watcher_tests.rs:5` — `use notify::event::ModifyKind`
