# Port report — `notify-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `notify`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 13 `use` sites from 1 other repos
- **Outbound**: 39 `use` sites to 3 other repos
- **Unresolved external**: 4 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `notify` | `notify` | 13 | 3 | COD-82 | 209 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (13 edges, 2 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-core` | 11 | ✅ |
| `codex-kotlin` | `core_test_support` | 2 | ✅ |

### Symbol-level demand (9 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `notify::RecursiveMode` | 3 | 3 | 0 | 2 |
| `notify::Watcher` | 3 | 3 | 0 | 2 |
| `notify::Event` | 1 | 1 | 0 | 1 |
| `notify::EventKind` | 1 | 1 | 0 | 1 |
| `notify::RecommendedWatcher` | 1 | 1 | 0 | 1 |
| `notify::event::AccessKind` | 1 | 1 | 0 | 1 |
| `notify::event::AccessMode` | 1 | 1 | 0 | 1 |
| `notify::event::CreateKind` | 1 | 1 | 0 | 1 |
| `notify::event::ModifyKind` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 9 symbols)

##### `notify::RecursiveMode` (3 sites, 3 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher.rs:18` — `use notify::RecursiveMode`
- `codex-kotlin/codex-core` `tests/common/lib.rs:390` — `use notify::RecursiveMode`
- `codex-kotlin/core_test_support` `lib.rs:390` — `use notify::RecursiveMode`

##### `notify::Watcher` (3 sites, 3 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher.rs:19` — `use notify::Watcher`
- `codex-kotlin/codex-core` `tests/common/lib.rs:391` — `use notify::Watcher`
- `codex-kotlin/core_test_support` `lib.rs:391` — `use notify::Watcher`

##### `notify::Event` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher.rs:14` — `use notify::Event`

##### `notify::EventKind` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher.rs:16` — `use notify::EventKind`

##### `notify::RecommendedWatcher` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher.rs:17` — `use notify::RecommendedWatcher`

##### `notify::event::AccessKind` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher_tests.rs:2` — `use notify::event::AccessKind`

##### `notify::event::AccessMode` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher_tests.rs:3` — `use notify::event::AccessMode`

##### `notify::event::CreateKind` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher_tests.rs:4` — `use notify::event::CreateKind`

##### `notify::event::ModifyKind` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/file_watcher_tests.rs:5` — `use notify::event::ModifyKind`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `windows-sys-kotlin` | 35 | 35 | `windows-sys` | COD-192 | 4 | To Do |
| `tempfile-kotlin` | 1 | 1 | `tempfile` | COD-149 | 108 | To Do |
| `walkdir-kotlin` | 3 | 1 | `walkdir` | COD-184 | 115 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `inotify` | 4 | 4 | `notify` |
| `kqueue` | 4 | 4 | `notify` |
| `notify-types` | 3 | 3 | `notify` |
| `fsevent-sys` | 2 | 2 | `notify` |
